#include "main.h"

/**
*_isupper - a function that checks for uppercase character
*@c: Argument to be passed into the function
*Return: 1 if successful otherwise 0 for failure
*/

int _isupper(int c)
{
	if
		(c >= 'A' && c <= 'Z')
			return (1);
	else
		return (0);
}
