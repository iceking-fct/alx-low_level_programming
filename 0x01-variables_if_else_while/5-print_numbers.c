#include <stdio.h>

/**
 * main - a code that prints all single digit numbers of base 10 from 0-9
 * Return: ALways 0 (done)
 */
int main(void)
{
	int i;

	for (i = 0 ; i <= 10 ; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
